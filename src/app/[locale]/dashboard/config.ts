'use client';

// This configuration file is used by all dashboard pages 
// to set them as dynamic rather than static pages

export const dynamic = 'force-dynamic';
export const dynamicParams = true; 